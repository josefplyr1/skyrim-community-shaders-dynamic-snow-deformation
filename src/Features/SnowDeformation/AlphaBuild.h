#pragma once

// Menu package switch. 0 = the Debugging Options tree is built; 1 = compiled
// out for public packages. Rewritten by Build-AlphaPackage.ps1, so this file
// stays ASCII-only.
#define SNOW_ALPHA_BUILD 0
